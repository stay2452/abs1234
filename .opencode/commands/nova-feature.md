---
description: Planeja uma nova feature respeitando as regras do projeto
agent: planejador
---

Planeje (sem editar nada ainda): $ARGUMENTS

O plano deve listar os arquivos a mexer em `src/app`, `src/components`, `src/lib` e `prisma/` (se precisar de schema, inclua a migration), citar as regras de `docs/CRITICAL_RULES.md` que se aplicam e confirmar: sem coleta real Apify, sem gasto de crédito, sem banco local, sem commit. Termine perguntando se pode executar.
